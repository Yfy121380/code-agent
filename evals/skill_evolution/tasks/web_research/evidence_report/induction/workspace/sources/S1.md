# S1: Dependency inputs

Source: https://example.invalid/reproducibility/dependencies

A reproducible build starts from an exact dependency lock rather than open version ranges. The lock should be reviewed when dependencies change. Build inputs should also identify the interpreter version used by the service.
